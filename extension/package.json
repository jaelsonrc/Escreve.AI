{
  "name": "escreve-ai-extension",
  "version": "1.0.0",
  "description": "Assistente de geração de texto contextual com IA para Chrome",
  "scripts": {
    "generate-icons": "node scripts/generate-icons.js",
    "build": "npm run generate-icons"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  }
}
